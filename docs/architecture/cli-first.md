# CLI-First Product Direction

OptiDock AI should feel native to the terminal before it grows into any broader interface surface.

## UX goals

- fast startup
- useful defaults
- readable findings
- benchmark-backed recommendations
- confidence before automation

## Principle

If a feature is important, it should be usable from the `optidock` CLI first.
